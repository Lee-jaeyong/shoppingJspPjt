<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="shopping.database.dao.BoardDAO">
	<select id="selectNow" resultType="string">
		select now()
	</select>

	<insert id="insert">
		insert into categorycheck values ('fdsfds',NULL);
		insert into smallcategory values (33,rew);
	</insert>
</mapper>
